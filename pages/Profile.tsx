import React from 'react';

// This file is obsolete and has been replaced by pages/Settings.tsx.
// It is populated with a placeholder component to prevent build errors
// in environments where the file cannot be deleted.
const Profile: React.FC = () => {
  return null; // Render nothing
};

export default Profile;
